# CSS Parser Fuzzer Cases

This repository collects minimized parser disagreement cases found by `css-fuzzer`.

Parser versions for the first run:

- postcss: 8.5.16
- prettier CSS parser: prettier 3.9.4, `__debug.parse(..., { parser: "css" })`
- lightningcss: 1.32.0
- oxc-css-parser: 0.0.5 at `oxc-project/oxc-css-parser@e4c405e`

## High Priority

| ID | Area | Minimal case | Why it matters | postcss | prettier CSS parser | lightningcss | oxc-css-parser |
| --- | --- | --- | --- | --- | --- | --- | --- |
| LC-SEL-001 | Selectors 5 column combinator | [`cases/high/selectors/lc-sel-001-column-combinator.css`](./cases/high/selectors/lc-sel-001-column-combinator.css) | `||` is the column combinator in current Selectors drafts; parser disagreement can break formatters and tools on valid selector syntax. | accepts | accepts | rejects: `Unexpected token in namespace selector: Delim('|')` | accepts |
| LC-IMPORT-001 | CSS Cascade 6 scoped import | [`cases/high/at-rules/lc-import-001-scoped-import.css`](./cases/high/at-rules/lc-import-001-scoped-import.css) | `@import ... scope(...)` is a current Cascade 6 scoped import form. lightningcss rejects the `scope()` import modifier while postcss, Prettier, and OXC accept the rule structure. | accepts | accepts | rejects: `Unexpected token Function("scope")` | accepts |
| LC-CONTAINER-001 | Container query condition list | [`cases/high/at-rules/lc-container-001-container-query-list.css`](./cases/high/at-rules/lc-container-001-container-query-list.css) | Current conditional/container examples include comma-separated query alternatives. lightningcss rejects the comma while postcss, Prettier, and OXC accept the rule structure. | accepts | accepts | rejects: `Unexpected token Comma` | accepts |
| LC-CONTAINER-002 | Container query custom property threshold | [`cases/high/at-rules/lc-container-002-container-query-var-threshold.css`](./cases/high/at-rules/lc-container-002-container-query-var-threshold.css) | The conditional/container spec corpus includes container query comparisons against `var()`. lightningcss rejects the `var()` threshold while postcss, Prettier, and OXC accept the rule structure. | accepts | accepts | rejects: `Unexpected token Function("var")` | accepts |
| ANCHOR-CONTAINER-001 | Anchor Position 2 anchored query | [`cases/high/at-rules/anchor-container-001-anchored-query.css`](./cases/high/at-rules/anchor-container-001-anchored-query.css) | CSS Anchor Position 2 includes `@container anchored(fallback: ...)` examples. lightningcss and OXC reject the `anchored()` condition while postcss and Prettier accept the rule structure. | accepts | accepts | rejects: `Unexpected token Function("anchored")` | rejects: `expect token ), but found <ident>` |
| ANCHOR-CONTAINER-002 | Anchor Position 2 anchored query with logical fallback | [`cases/high/at-rules/anchor-container-002-anchored-self-block-start.css`](./cases/high/at-rules/anchor-container-002-anchored-self-block-start.css) | CSS Anchor Position 2 examples include `anchored(fallback: self-block-start)`. This variant isolates the lightningcss rejection while OXC accepts the anchored query syntax. | accepts | accepts | rejects: `Unexpected token Function("anchored")` | accepts |
| LC-VAR-001 | CSS Variables 2 nested `var()` reference | [`cases/high/values/lc-var-001-nested-var-reference.css`](./cases/high/values/lc-var-001-nested-var-reference.css) | CSS Variables 2 allows the custom property name looked up by a `var()` to itself come from `var()`. lightningcss rejects the nested form while postcss, Prettier, and OXC accept it. | accepts | accepts | rejects: `Unexpected token Function("var")` | accepts |
| OXC-CONTAINER-001 | Container query by name only | [`cases/high/at-rules/oxc-container-001-name-only-query.css`](./cases/high/at-rules/oxc-container-001-name-only-query.css) | CSS Conditional 5 says it is possible to query for a container only by name. OXC rejects the name-only `@container` form while the other parsers accept it. | accepts | accepts | accepts | rejects: `expect token <ident>, but found {` |
| OXC-SCROLL-001 | Scroll-driven keyframe range selector | [`cases/high/at-rules/oxc-scroll-001-keyframes-entry-range.css`](./cases/high/at-rules/oxc-scroll-001-keyframes-entry-range.css) | Scroll-driven animations allow timeline range names such as `entry` in keyframe selectors. OXC rejects `entry 0%` while the other parsers accept it. | accepts | accepts | accepts | rejects: `expect token :, but found <percentage>` |
| OXC-SHADOW-PARTS-001 | CSS Shadow Parts multi-ident `::part()` | [`cases/high/selectors/oxc-shadow-parts-001-multiple-part-ident.css`](./cases/high/selectors/oxc-shadow-parts-001-multiple-part-ident.css) | CSS Shadow Parts allows multiple idents inside `::part()`. OXC rejects the second ident while postcss, Prettier, and lightningcss accept the selector. | accepts | accepts | accepts | rejects: `expect token ), but found <ident>` |
| OXC-COLOR-001 | CSS Color 5 `device-cmyk` color profile | [`cases/high/at-rules/oxc-color-001-device-cmyk-profile.css`](./cases/high/at-rules/oxc-color-001-device-cmyk-profile.css) | CSS Color 5 includes `@color-profile` and `device-cmyk`; OXC reports a recoverable parser error for the predefined profile name while the other parsers accept it. | accepts | accepts | accepts | accepts with errors: `dashed identifier is expected` |
| LC-SEL-002 | Selectors 5 reference combinator | [`cases/high/selectors/lc-sel-002-reference-combinator.css`](./cases/high/selectors/lc-sel-002-reference-combinator.css) | Selectors 5 defines the `/for/` reference combinator. lightningcss rejects it as a dangling combinator while postcss, Prettier, and OXC accept the selector. | accepts | accepts | rejects: `Invalid dangling combinator in selector` | accepts |
| LC-PSEUDO-001 | CSS Pseudo 4 generated marker chain | [`cases/high/selectors/lc-pseudo-001-before-marker-chain.css`](./cases/high/selectors/lc-pseudo-001-before-marker-chain.css) | CSS Pseudo 4 explicitly allows `::before::marker`. lightningcss rejects the pseudo-element chain while postcss, Prettier, and OXC accept it. | accepts | accepts | rejects: `Invalid state` | accepts |
| LC-SCOPE-001 | CSS Cascade 6 scoped relative selector | [`cases/high/at-rules/lc-scope-001-relative-selector.css`](./cases/high/at-rules/lc-scope-001-relative-selector.css) | CSS Cascade 6 says style rules inside `@scope` use relative selectors by default and shows `> p` as an example. lightningcss rejects the explicit combinator form. | accepts | accepts | rejects: `Invalid empty selector` | accepts |
| LC-SCOPE-002 | CSS Cascade 6 empty scope roundtrip | [`cases/high/at-rules/lc-scope-002-empty-scope-roundtrip.css`](./cases/high/at-rules/lc-scope-002-empty-scope-roundtrip.css) | lightningcss accepts `@scope (.x || a)` and emits `@scope ()`, which the parser matrix no longer agrees on. This is an accepted-input/emitted-output roundtrip problem, not just a raw invalid input. | accepts | accepts | accepts | rejects: `simple selector is expected` |

## Medium Priority

| ID | Area | Minimal case | Why it matters | postcss | prettier CSS parser | lightningcss | oxc-css-parser |
| --- | --- | --- | --- | --- | --- | --- | --- |
| LC-PROP-001 | CSS Properties and Values API `@property` validation | [`cases/medium/at-rules/lc-prop-001-property-initial-value-validation.css`](./cases/medium/at-rules/lc-prop-001-property-initial-value-validation.css) | The `initial-value` does not match `syntax: "<color>"`; lightningcss validates and rejects, while parser-only tools accept. This affects whether the fuzzer should classify descriptor validation separately from syntax parsing. | accepts | accepts | rejects: `Unexpected token Function("calc")` | accepts |
| LC-PROP-002 | CSS Properties and Values API relative color validation | [`cases/medium/at-rules/lc-prop-002-property-relative-hsl-validation.css`](./cases/medium/at-rules/lc-prop-002-property-relative-hsl-validation.css) | The `initial-value` does not match `syntax: "<length>"`; lightningcss validates the descriptor and rejects the relative `hsl()` color. Plain declaration use of the same relative color is accepted by all parsers. | accepts | accepts | rejects: `Unexpected token Function("hsl")` | accepts |
| LC-PROP-003 | CSS Properties and Values API container unit validation | [`cases/medium/at-rules/lc-prop-003-property-cqi-validation.css`](./cases/medium/at-rules/lc-prop-003-property-cqi-validation.css) | The `initial-value` uses a container query unit while the registered syntax is `<color>`. lightningcss validates and rejects; parser-only tools accept the rule structure. | accepts | accepts | rejects: `Unexpected token Dimension ... unit: "cqi"` | accepts |
| LC-PROP-004 | CSS Properties and Values API `rgb()` validation | [`cases/medium/at-rules/lc-prop-004-property-rgb-validation.css`](./cases/medium/at-rules/lc-prop-004-property-rgb-validation.css) | The `initial-value` is an `rgb()` color while the registered syntax is `<length>`. lightningcss validates and rejects; parser-only tools accept. | accepts | accepts | rejects: `Unexpected token Function("rgb")` | accepts |
| LC-PROP-005 | CSS Properties and Values API `color-mix()` validation | [`cases/medium/at-rules/lc-prop-005-property-color-mix-validation.css`](./cases/medium/at-rules/lc-prop-005-property-color-mix-validation.css) | The `initial-value` is a `color-mix()` color while the registered syntax is `<length>`. Plain declaration use of `color-mix()` is accepted by all parsers; this is descriptor validation. | accepts | accepts | rejects: `Unexpected token Function("color-mix")` | accepts |
| DRAFT-PAGE-001 | CSS Content/Page `@top` margin rule | [`cases/medium/at-rules/draft-page-001-top-margin-rule.css`](./cases/medium/at-rules/draft-page-001-top-margin-rule.css) | The CSS Content 3 corpus includes `@top` nested in `@page:left` and `@page:right`. lightningcss rejects `@top`, while postcss, Prettier, and OXC accept the rule structure. | accepts | accepts | rejects: `Unknown at rule: @top` | accepts |
| DRAFT-TEMPLATE-001 | CSS Template `@page::slot()` syntax | [`cases/medium/at-rules/draft-template-001-page-slot-pseudo.css`](./cases/medium/at-rules/draft-template-001-page-slot-pseudo.css) | CSS Template Layout 1 includes `@page::slot(g)` examples. lightningcss and OXC reject the pseudo syntax while parser-only tools accept it. | accepts | accepts | rejects: `Unexpected token Colon` | rejects: `expect token ;, but found ::` |
| DRAFT-SLOT-001 | CSS GCPM/Page `@slot` rule | [`cases/medium/at-rules/draft-slot-001-page-slot-rule.css`](./cases/medium/at-rules/draft-slot-001-page-slot-rule.css) | CSS GCPM 4 proposes `@slot` inside `@page` for page-generated slots. lightningcss rejects the nested `@slot`, while postcss, Prettier, and OXC accept the rule structure. | accepts | accepts | rejects: `Unknown at rule: @slot` | accepts |
| LC-LINKPARAMS-001 | CSS Link Parameters URL comma form | [`cases/medium/values/lc-linkparams-001-url-param-comma.css`](./cases/medium/values/lc-linkparams-001-url-param-comma.css) | CSS Link Parameters 1 includes `url("image.svg", param(...))`. lightningcss rejects the comma form while postcss, Prettier, and OXC accept it. | accepts | accepts | rejects: `Unexpected token Comma` | accepts |
| LC-LINKPARAMS-002 | CSS Link Parameters URL function form | [`cases/medium/values/lc-linkparams-002-url-param-function.css`](./cases/medium/values/lc-linkparams-002-url-param-function.css) | CSS Link Parameters 1 includes URL values followed by `param(...)`. lightningcss rejects the function form while postcss, Prettier, and OXC accept it. | accepts | accepts | rejects: `Unexpected token Function("param")` | accepts |

## Low Priority

| ID | Area | Minimal case | Why it matters | postcss | prettier CSS parser | lightningcss | oxc-css-parser |
| --- | --- | --- | --- | --- | --- | --- | --- |
| STRICT-SEL-001 | Invalid selector recovery | [`cases/low/selectors/strict-sel-001-double-class-dot.css`](./cases/low/selectors/strict-sel-001-double-class-dot.css) | `.a..b` is invalid selector syntax. postcss and Prettier accept it, while strict parsers reject it. Keep as a recovery/leniency reference, not a likely strict-parser bug. | accepts | accepts | rejects: `Expected identifier in class selector, got Delim('.')` | rejects: `expect token <ident>, but found .` |
| STRICT-MQ-001 | Invalid media query recovery | [`cases/low/media-queries/strict-mq-001-min-width-range-mix.css`](./cases/low/media-queries/strict-mq-001-min-width-range-mix.css) | `(min-width > 1024px)` mixes the legacy `min-` prefix with range syntax; `(width > 1024px)` is accepted by all parsers. Keep as a leniency reference. | accepts | accepts | rejects: `Invalid media query` | accepts |
| STRICT-IMPORT-001 | Late `@import` recovery | [`cases/low/at-rules/strict-import-001-late-import.css`](./cases/low/at-rules/strict-import-001-late-import.css) | CSS requires `@import` to precede ordinary style rules. lightningcss rejects a late import while parser-only tools accept it. | accepts | accepts | rejects: `@import rules must precede all rules aside from @charset and @layer statements` | accepts |
| STRICT-VAR-001 | Dynamic property name recovery | [`cases/low/declarations/strict-var-001-dynamic-property-name.css`](./cases/low/declarations/strict-var-001-dynamic-property-name.css) | CSS Values 5 shows that `var()` cannot be used as a property name and the declaration should be thrown away as a syntax error. postcss and Prettier accept the stylesheet; lightningcss and OXC reject. | accepts | accepts | rejects: `Unexpected token Semicolon` | rejects: `expect token :, but found (` |
| STRICT-URL-001 | `var()` inside legacy `url()` recovery | [`cases/low/values/strict-url-001-var-in-url.css`](./cases/low/values/strict-url-001-var-in-url.css) | CSS Values 4 says unquoted `url()` is specially parsed and cannot provide a URL by functions such as `var()`. lightningcss rejects the bad URL token while parser-only tools accept. | accepts | accepts | rejects: `Unexpected token BadUrl("var(--foo")` | accepts |
| STRICT-FONT-001 | Invalid unquoted font family punctuation | [`cases/low/values/strict-font-001-unquoted-family-punctuation.css`](./cases/low/values/strict-font-001-unquoted-family-punctuation.css) | CSS Fonts 4 lists unquoted font family names with punctuation such as `Ahem!` as invalid. lightningcss rejects the declaration value while parser-only tools accept. | accepts | accepts | rejects: `Unexpected token Delim('!')` | accepts |
| STRICT-MQ-002 | Invalid media query list recovery | [`cases/low/media-queries/strict-mq-002-invalid-query-list-recovery.css`](./cases/low/media-queries/strict-mq-002-invalid-query-list-recovery.css) | Media Queries 5 says a grammar mismatch in one media query is replaced by `not all` and does not wipe out the whole list. Strict parsers reject this malformed-list example. | accepts | accepts | rejects: `Unexpected token Comma` | rejects: `expect token <ident>, but found &` |
| STRICT-MQ-003 | Invalid media at-rule semicolon recovery | [`cases/low/media-queries/strict-mq-003-invalid-media-semicolon.css`](./cases/low/media-queries/strict-mq-003-invalid-media-semicolon.css) | `@media test;` is invalid media at-rule syntax. postcss and Prettier recover from the semicolon form, while strict parsers reject it. | accepts | accepts | rejects: `Unexpected token Semicolon` | rejects: `expect token {, but found ;` |

## Verification

Run the fuzzer verifier after changing cases:

```sh
vp run verify-cases -- --dir ../css-parser-fuzzer-cases/cases --readme ../css-parser-fuzzer-cases/README.md --timeout-ms 1500 --check-minimized --minimize-attempts 80
```

The verifier checks sidecar note coverage, sidecar reproduction blocks, sidecar required headings with non-empty `Spec Context` or `Triage Note` explanatory text, sidecar title IDs, README coverage, stale links, unique case listing, case ID filename prefixes, High/Medium/Low priority grouping, priority folder consistency, complete README and sidecar parser matrix coverage, parser matrix drift, continued parser disagreement, and whether the current structure-level reducer can still produce a shorter case with the same parser-result fingerprint. For recognized issue families, the reducer must preserve that family as well as the parser fingerprint.

## Triage Rules

- High priority: current or near-current CSS syntax where at least two parsers accept the input and one production parser rejects it, or any parser crashes or times out.
- Medium priority: draft syntax with unclear implementation status, recovery-mode differences that may still affect formatter output, or parser-specific extensions.
- Low priority: likely-invalid syntax, duplicate findings, or behavior that needs a stronger spec citation before escalation.
